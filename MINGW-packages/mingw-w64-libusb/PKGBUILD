# build command: makepkg-mingw

_realname=libusb-usbdk
pkgname="${MINGW_PACKAGE_PREFIX}-${_realname}"
pkgver=0.0.0
pkgrel=1
pkgdesc="Library that provides generic access to USB devices'. (mingw-w64)"
arch=('any')
url="http://libusb.info/"
license=("LGPL")
makedepends=("${MINGW_PACKAGE_PREFIX}-gcc")
options=('staticlibs' 'strip')
source=("https://github.com/libusb/libusb/archive/448584afd1b012210e8e9038f888597588450d7a.zip")
md5sums=('7c2fdea289cfe10952b11b76a0df3d2f')

prepare() {
  cd "${srcdir}"
  rm -rf "${_realname}-${pkgver}/"
  mv "libusb-448584afd1b012210e8e9038f888597588450d7a" "${_realname}-${pkgver}"
  cd "${_realname}-${pkgver}"
  ./bootstrap.sh
}

build() {
  #export lt_cv_deplibs_check_method='pass_all'
  mkdir -p "${srcdir}/build-${MINGW_CHOST}"
  cd "${srcdir}/build-${MINGW_CHOST}"
  "${srcdir}"/${_realname}-${pkgver}/configure \
    --prefix=${MINGW_PREFIX} \
    --build=${MINGW_CHOST} \
    --host=${MINGW_CHOST} \
    --enable-shared \
    --enable-static \
    --enable-usbdk

  make
}

check() {
  cd "${srcdir}/build-${MINGW_CHOST}"
  make check
}

package() {
  cd "${srcdir}/build-${MINGW_CHOST}"
  make DESTDIR="$pkgdir" install
}
